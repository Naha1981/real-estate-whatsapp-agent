# iGosa — WhatsApp AI Platform for Estate Agents
# Core dependencies (Render free tier optimized)
# Pin only what's needed — let pip resolve compatible versions

# Web framework
fastapi>=0.110,<0.120
uvicorn[standard]>=0.29,<0.31
python-multipart>=0.0.9

# Evolution API HTTP client
httpx>=0.27
tenacity

# AI / LLM
openai>=1.40

# Database
sqlalchemy>=2.0
aiosqlite>=0.20

# Utilities
pydantic>=2.0
python-dotenv>=1.0
python-dateutil

# Dev
pytest
