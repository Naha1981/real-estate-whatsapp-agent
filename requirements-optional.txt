# iGosa — Optional/heavy dependencies
# Install separately when needed (not for Render free tier initial deploy)

# Vector search (heavy — needs Rust toolchain)
# chromadb==0.5.5

# Background tasks (needs Redis server)
# redis==5.1.1
# celery[redis]==5.4.0

# Voice transcription (heavy — needs PyTorch, ffmpeg)
# openai-whisper==20231117

# PDF generation
# weasyprint==62.3

# Image processing
# pillow==10.4.0

# Token counting
# tiktoken==0.7.0

# Database migrations
# alembic==1.13.2

# Advanced config
# pydantic-settings==2.5.2
